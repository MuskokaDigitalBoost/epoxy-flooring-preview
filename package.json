{
  "name": "northline-floor-co-concept",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-site.mjs",
    "validate:starter": "node scripts/validate-site.mjs --allow-placeholders",
    "check": "npm run build && npm run validate",
    "check:starter": "npm run build && npm run validate:starter"
  },
  "engines": {
    "node": ">=20.11"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
